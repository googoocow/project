@using PhaseTwo.Helpers; 
<ul class="nav navbar-nav navbar-right">
	<li>@Html.ActionLink("Logout", "Index", "Logout").If(User.Identity.IsAuthenticated)</li>
    <li>@Html.Partial("~/Views/Login/Index.cshtml").If(!User.Identity.IsAuthenticated)</li>
    <li>@Html.ActionLink("Register", "Register", "Register")</li>
</ul>

